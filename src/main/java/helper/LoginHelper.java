package helper;

/*
import org.openqa.selenium.By;
import util.ObjectRepo;
import util.PropertyReader;

public class LoginHelper {

    public static void loginToCPQUsing(String account_name, String opportunity) {

        TextHelper.sendInputText(By.xpath(""),PropertyReader.getUsername());
        TextHelper.sendInputText(By.xpath(""),PropertyReader.getPassword());
        ButtonHelper.clickButton(PropertyReader.getLoginButton());
        ObjectRepo.waitForLoad(10000);
        try{
            ButtonHelper.clickButton(PropertyReader.getPopupButton());
        }catch (Exception e){
            e.printStackTrace();
        }
        ButtonHelper.clickButton(PropertyReader.getCustomerDrop());
        ButtonHelper.clickButton(PropertyReader.getAccounts());
        ObjectRepo.waitForLoad(10000);
        ButtonHelper.clickButton(PropertyReader.getMainSearchButton());
    }
}
*/